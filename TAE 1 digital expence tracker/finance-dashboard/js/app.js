// =========================================
// Digital Personal Finance Dashboard
// Main JavaScript File
// =========================================

// ---------- State ----------
// (In a later phase, this is where we'll store our
// transactions array and savings goals array.)


// ---------- DOM Elements ----------
// (In a later phase, this is where we'll grab references
// to HTML elements we need to read from or update.)


// ---------- Initialization ----------
function init() {
  console.log("Finance Dashboard app.js is connected and running!");
}

// Wait until the HTML has fully loaded before running our code.
// This prevents errors from trying to grab elements that don't exist yet.
document.addEventListener("DOMContentLoaded", init);